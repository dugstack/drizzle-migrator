---
name: drizzle-migrator
description: Operating manual for @yourorg/drizzle-migrator (placeholder npm scope) — how AI agents should add, run, and adopt migrations. Full content lands in Milestone 6.
---

# drizzle-migrator (placeholder)

The bundled agent skill for `@yourorg/drizzle-migrator` is written in **Milestone 6** of the
package plan (§15): required context, workflow, migration entry pattern, breakpoints and data
remaps, operational rules, adoption rules, verification SQL, and the command table (kept in sync
with the CLI dispatch table by a dedicated test).

This placeholder exists so the shipped artifact layout — `skills/drizzle-migrator/SKILL.md`,
included in the npm tarball via `files: ["dist", "skills"]` — is in place from Milestone 1.
